server:
  port: 8081
  servlet:
    context-path: /desafio
  
client:
  protocol: http
  host: localhost
  port: 8080
  context: /periodos
  api-path: /api
  
  
springfox:
  documentation:
    swagger:
      v2:
        path: /api-docs
        
management:
  endpoints:
    web:
      base-path: /
      
info:
  app:
    name: ^project.name^
    description: Prueba Técnica
    version: ^project.version^
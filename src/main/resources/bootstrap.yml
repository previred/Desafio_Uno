server:
  port: 8081
  servlet:
    context-path: /periodos
spring:
  application:
    name: periodos
  cloud:
    config:
      enabled: false
      failFast: true
management:
  security:
    enabled: false

logging:
  level:
    org:
      springframework:
        web: TRACE